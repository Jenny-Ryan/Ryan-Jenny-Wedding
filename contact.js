/* Toggle the show-menu class on the header when the menu button is clicked */
function toggleMenu() {
    var header = document.querySelector('.mobile-nav');
    header.classList.toggle('show-menu');
  }
